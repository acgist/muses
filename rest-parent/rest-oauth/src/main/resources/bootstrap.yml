spring:
  application:
    name: rest-oauth
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev, redis
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: localhost:8848
