package com.acgist.admin.${modulePath}.service;

import com.acgist.admin.data.${modulePath}.entity.${prefix};
import com.acgist.boot.service.BootService;

/**
 * ${name}
 * 
 * @author ${author}
 */
public interface ${prefix}Service extends BootService<${prefix}> {

}
