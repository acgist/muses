spring:
  application:
    name: boot-redis
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  redis:
    host: localhost
    port: 6379
    database: 0
    password: 
    timeout: 4000
    pool.max-wait: -1
    pool.min-idle: 0
    pool.max-idle: 10
    pool.max-active: 100
    