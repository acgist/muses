spring:
  application:
    name: service-pay
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev, data
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: localhost:8848
      config:
        ext-config:
          - data-id: redis.properties
            refresh: true
            group: DEFAULT_GROUP