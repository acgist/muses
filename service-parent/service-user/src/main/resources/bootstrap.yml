spring:
  application:
    name: service-pay
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev, data, redis
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: localhost:8848